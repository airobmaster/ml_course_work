# Submission Format

The format for the submission file is two columns with the `building_id` and the `damage_grade`.

The data type of `damage_grade` is an integer with values of `1`, `2`, `3`, so make sure there is no decimal point and no other numbers in your submission.

For example, `1` would be valid, and `1.0` would not.
